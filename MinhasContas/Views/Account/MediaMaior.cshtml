@model minhas_contas.Models.Account
<div>
    <table class="table">
        <thead>
            <tr>
                <th>Maior Consumo</th>
                <th></th>
            </tr>
             @foreach(var item in Model)
             {
                 <tr>
                     <td>@item.MaiorConsumo</td>
                 </tr>
             }
        </thead>
    </table>
</div>